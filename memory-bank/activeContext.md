# Active Context

## Current Work Focus
Initial project setup: Next.js initialization, Git initialization, and setting up the Memory Bank.

## Next Steps
- Commit the initial state and Memory Bank files to Git.
- Configure Firebase project and add Firebase SDK to the Next.js app.
- Set up Tailwind CSS theme configuration (colors and fonts).
- Implement the basic Layout (Navbar, Hamburger Menu).
- Create Admin login (`/login`) & Auth guard for `/admin`.

## Active Decisions
- A product can belong to multiple categories.
- Products have a single price.
- We will add an `order` field to sort categories and products.
- No direct online order cart, just phone call via CTA.
